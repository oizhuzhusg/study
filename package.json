{
  "name": "chem-coach",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:production": "wrangler deploy --env production",
    "check": "node --check worker.js && node --check src/worker/*.js && node --check src/shared/*.js"
  },
  "devDependencies": {
    "wrangler": "^4.85.0"
  }
}
